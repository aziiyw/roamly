// Template for config.js — copy this file to config.js and fill in glmApiKey.
// See config.js for the full security warning. In short: the key is public,
// so use a throwaway Z.AI key with a low quota.
window.VERVIA_CONFIG = {
  glmApiKey: 'PASTE-YOUR-THROWAWAY-ZAI-KEY-HERE',
  glmModel: 'glm-5v-turbo',
  glmEndpoint: 'https://api.z.ai/api/paas/v4/chat/completions'
};
